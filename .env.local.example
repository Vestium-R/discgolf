# Admin password to add rounds / manage roster
ADMIN_PASSWORD=change-me

# Upstash Redis (get from Vercel -> Storage -> KV, or upstash.com)
# If these are blank, the app uses an in-memory store (data resets on redeploy).
KV_REST_API_URL=
KV_REST_API_TOKEN=
